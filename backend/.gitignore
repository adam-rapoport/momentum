.venv/
__pycache__/
*.pyc
.env
.pytest_cache/
*.egg-info/

# Local SQLite databases (the default DB for the desktop build)
*.db
*.db-journal
*.db-wal

# PyInstaller output (the frozen desktop sidecar). Regenerated from
# pmomentum.spec via `pyinstaller pmomentum.spec`; not committed.
/build/
/dist/
