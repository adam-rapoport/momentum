"""Third-party integrations.

Sprint 3 Chunk D ships the Google Docs integration. Each provider lives
in its own submodule; the shared Fernet-backed credential vault lives in
`vault.py` so tokens are never stored in plaintext.
"""
