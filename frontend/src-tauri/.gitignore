# Generated by Cargo
# will have compiled files and executables
/target/
/gen/schemas

# Bundled Python sidecar (copied in from backend/dist via PyInstaller). It's a
# large (~70MB) build artifact regenerated per build, not committed.
/binaries/
